// 화살표 함수
// ES6 신 문법
function add(x, y) {
  return x + y;
}

// 1-1. 기본적인 화살표 함수
let arrowFunc01 = (x, y) => {
  return x + y;
};

//1-2. 한 줄로
let aroowFunc02 = (x, y) => x + y;

function testFunc(x) {
  return x;
}

//화살표 함수로!
let arrowFunc03 = (x) => x;

// 정석대로 사용하다가 익숙해지면 화살표함수로 넘여가는 것을 추천!
